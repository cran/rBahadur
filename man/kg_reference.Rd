% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{kg_reference}
\alias{kg_reference}
\title{Load the bundled 1000 Genomes reference panel}
\source{
Phase 3 of the 1000 Genomes Project, GRCh38, chromosome 22
positions 20,000,000 to 21,000,000, restricted to biallelic single
nucleotide variants with minor allele frequency at least 0.05 in the first
260 samples. Genetic map positions are interpolated from the GRCh38 PLINK
maps distributed with BEAGLE.
}
\usage{
kg_reference()
}
\value{
A list with \code{haplotypes} (520 by 2500 raw matrix of 0/1), \code{pos},
\code{cM}, \code{chrom}, \code{build}, and \code{source}.
}
\description{
A small real reference panel, included so that examples, tests, and the
vignette run offline. It covers a 1 Mb window of chromosome 22 and is
intended for demonstration rather than analysis; use \code{\link[=vcf_to_panel]{vcf_to_panel()}} or
\code{\link[=download_1kg_panel]{download_1kg_panel()}} to build a panel at realistic scale.
}
\examples{
panel <- kg_reference()
dim(panel$haplotypes)
range(panel$pos)

## realistic local LD is the point: correlation decays with distance
H <- matrix(as.integer(panel$haplotypes), nrow = nrow(panel$haplotypes))
cor(H[, 1], H[, 2])^2
cor(H[, 1], H[, 2000])^2
}
