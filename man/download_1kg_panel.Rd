% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{download_1kg_panel}
\alias{download_1kg_panel}
\title{Download a 1000 Genomes region and build a reference panel}
\usage{
download_1kg_panel(
  chrom = "22",
  start = 2e+07,
  end = 2.1e+07,
  dest = tempdir(),
  ...
)
}
\arguments{
\item{chrom}{autosome, as a string from \code{"1"} through \code{"22"}}

\item{start, end}{base pair bounds of the region to keep}

\item{dest}{directory in which to cache downloads. Defaults to a temporary
directory.}

\item{...}{further arguments passed to \code{\link[=vcf_to_panel]{vcf_to_panel()}}, such as \code{min_maf}
and \code{max_samples}}
}
\value{
A panel list suitable for \code{\link[=am_mosaic]{am_mosaic()}}.
}
\description{
Convenience wrapper that fetches a window of phased 1000 Genomes data along
with a genetic map and hands both to \code{\link[=vcf_to_panel]{vcf_to_panel()}}. It exists so the
vignette's workflow can be reproduced at realistic scale; it requires
network access and downloads a large file, so it is not run in examples or
tests.
}
\details{
Streams the VCF and stops reading once \code{end} is passed, so a small
window costs far less than the whole chromosome. The data are the GRCh38
phased biallelic release, and the genetic map is the GRCh38 PLINK map
distributed with BEAGLE. A new cache file is published only after the
stream reaches a variant beyond \code{end}, preventing an interrupted download
from being reused as a complete region.
}
\examples{
\donttest{
## requires network access
if (interactive()) {
  panel <- download_1kg_panel("22", 20e6, 20.5e6, max_samples = 200)
  dim(panel$haplotypes)
}
}
}
