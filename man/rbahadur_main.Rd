% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{rbahadur_main}
\alias{rbahadur_main}
\title{Run the \code{rbahadur} command line interface}
\usage{
rbahadur_main(args = commandArgs(trailingOnly = TRUE))
}
\arguments{
\item{args}{character vector of command line arguments, as returned by
\code{commandArgs(trailingOnly = TRUE)}}
}
\value{
Invisibly, an integer exit status: \code{0} on success, \code{1} for a usage
error, and \code{2} for a runtime failure. Usage text goes to stdout when
requested and to stderr when the invocation was wrong.
}
\description{
Dispatches a vector of command line arguments. This is what the shipped
\code{rbahadur} script calls; it is exported so the interface can be driven from
R and tested. It returns an exit status rather than terminating the session.
}
\examples{
p <- file.path(tempdir(), "cli_example")
rbahadur_main(c("simulate", "--h2", "0.5", "--r", "0.3",
                "--m", "50", "--n", "40", "--out", p, "--quiet"))
rbahadur_main(c("info", p))
}
