% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mosaic.R
\name{am_mosaic}
\alias{am_mosaic}
\title{Simulate equilibrium assortative mating with realistic local LD}
\usage{
am_mosaic(
  h2_0,
  r,
  n,
  panel,
  causal_idx = NULL,
  m = NULL,
  path = NULL,
  format = c("individual", "variant", "bed"),
  batch_size = NULL
)
}
\arguments{
\item{h2_0}{generation zero (panmictic) heritability, in \verb{(0, 1)}}

\item{r}{cross-mate phenotypic correlation, in the open interval (-1, 1).
Negative values give disassortative mating.}

\item{n}{positive whole-number count of individuals to simulate}

\item{panel}{reference panel: a list with \code{haplotypes} (a haplotypes by
markers matrix of 0 and 1, optionally stored as \code{raw}), \code{pos} (strictly
increasing base pair positions), and optionally \code{cM} (genetic map position
of each marker), \code{chrom}, \code{id}, \code{ref}, and \code{alt}. Marker metadata can have
length one or one entry per marker and is carried into PLINK \code{.bim} output.
Haplotype value 0 denotes the reference allele and value 1 the alternate
allele. Without \code{cM}, breakpoints are drawn uniformly in physical distance.
See \code{\link[=kg_reference]{kg_reference()}} for the bundled example.}

\item{causal_idx}{integer indices of the markers to treat as causal. If
\code{NULL}, \code{m} evenly spaced markers are used.}

\item{m}{whole-number count of causal variants, used only when \code{causal_idx}
is \code{NULL}}

\item{path, format, batch_size}{streaming options, exactly as in
\code{\link[=am_simulate]{am_simulate()}}. With \code{path = NULL} the genotype matrix is returned in
memory; otherwise it is streamed to disk and omitted from the result.}
}
\value{
A list with \code{y}, \code{g}, \code{AF} (allele frequencies at the causal loci),
\code{beta_std}, \code{beta_raw}, \code{causal_idx}, and \code{pos}. With \code{path = NULL} it also
carries \code{X}, an \code{n} by \code{p} integer matrix of diploid genotypes at every
panel marker. With \code{path} supplied, \code{X} is omitted and \code{path}, \code{format},
\code{n}, \code{m}, \code{h2_0}, and \code{r} are added, where \code{m} counts all panel markers
written rather than only the causal ones.
}
\description{
Combines the global linkage disequilibrium induced by assortative mating
with the local linkage disequilibrium induced by limited recombination.
Causal variants are drawn with \code{\link[=rb_dplr]{rb_dplr()}}, giving the dense genome-wide
structure assortative mating produces, and the remaining markers are filled
in by copying contiguous haplotype blocks from a reference panel, giving
realistic short-range structure. Block boundaries are sampled from the
panel's genetic map, so breakpoints concentrate where recombination is high.
}
\details{
Genotypes at the causal loci are exactly what \code{\link[=rb_dplr]{rb_dplr()}} drew, so
the equilibrium relationships in \code{\link[=h2_eq]{h2_eq()}} and \code{\link[=vg_eq]{vg_eq()}} hold there, while
surrounding markers inherit the panel's correlation structure. Combining
the two rather than approximating them jointly sidesteps the feasibility
limit in \code{\link[=am_covariance_structure]{am_covariance_structure()}}: representing strong local LD and
genome-wide assortative mating in a single Bahadur order-2 distribution is
generally not possible.

The equilibrium formulas are large-locus results. Calls with fewer than 50
causal variants are allowed but warn because realized variances can differ
materially from the targets. Set
\code{options(rBahadur.warn_small_m = FALSE)} to silence this warning after
deciding that the finite-locus approximation is appropriate.

Each block is copied from one panel haplotype, so within a block the
simulated data reproduces panel LD exactly, while correlation across a
breakpoint is broken apart from what the causal variants carry. More
causal variants therefore means more, shorter blocks.

Because the panel is finite, the simulated data cannot contain haplotypes
the panel does not, and a small panel will show inflated identity by
descent between simulated individuals.
}
\examples{
panel <- kg_reference()
sim <- am_mosaic(h2_0 = 0.5, r = 0.4, n = 50, panel = panel, m = 50)
dim(sim$X)

## neighbouring markers are correlated because they are copied together,
## which is the local LD that am_simulate() cannot produce
j <- sim$causal_idx[10]
cor(sim$X[, j], sim$X[, j + 1])
}
\seealso{
\code{\link[=am_simulate]{am_simulate()}} for the unlinked-loci case, and \code{\link[=kg_reference]{kg_reference()}}
for the bundled 1000 Genomes panel.
}
